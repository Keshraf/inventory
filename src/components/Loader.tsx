import Logo from "./Logo";

const Loader = () => {
  return (
    <main className="w-full h-screen bg-white flex flex-row items-center justify-center">
      <Logo />
    </main>
  );
};

export default Loader;
